#include <stdio.h>
#include <string.h>

/**
 * main - return string
 * Return: 1 if the printf function is true
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
